type CatchmentFeature @model(class: "\\App\\Models\\Catchment") {
    type: String
    geometry: GeoJSONGeometry
    properties: CatchmentProperties
}

type CatchmentProperties {
    id: ID
    name: String
    label: String
    nameShort: String
    code: String
    state: String
    slug: String
}

extend type Query {
    catchment(id: ID! @eq): CatchmentFeature @find

    catchmentsByName(
        name: String! @ilike(key: "name", template: "{}%")
    ): [CatchmentFeature] @all(model: "App\\Models\\Catchment")

    catchmentsByPoint(
        latitude: Float!
        longitude: Float!
    ): [CatchmentFeature]

    catchmentBySlug(slug: String! @eq): CatchmentFeature @find

    catchmentByName(name: String! @eq): CatchmentFeature @find
}